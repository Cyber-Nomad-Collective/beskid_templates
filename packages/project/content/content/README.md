# {{name}}

Authoring package for Beskid template `{{shortName}}`.

1. Edit `.beskid/template.json` (symbols, sources, tags).
2. Add scaffold files under `content/`.
3. Pack and publish when `beskid pckg pack` supports `packageKind: template`.
